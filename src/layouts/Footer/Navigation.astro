---
const linkArray = [
  { name: "One", path: "/" },
  { name: "Two", path: "/" },
];
---

<nav class="flex items-center gap-XXXXX">

  {linkArray.map((current) => (
    <a href={current.path} class="hover:text-clr-hover"  >
      {current.name}
    </a>
  ))}

</nav>

