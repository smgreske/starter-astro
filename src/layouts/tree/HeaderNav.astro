---
import { Icon } from 'astro-icon'

const linkArray = [
  { name: "About", path: "/" },
  { name: "Services", path: "/" },
];
---

<nav class="flex items-center gap-9 sm:flex-col sm:gap-16">

  {linkArray.map((current) => (
    <a href={current.path} class="text-clr-n2 hover:text-clr-n1"  >
      {current.name}
    </a>
  ))}

    <a class='flex items-center gap-2 border px-2 py-1 transition-all
      text-clr-n2 hover:bg-clr-n1 hover:text-clr-n4 hover:fill-clr-n4'  href="#">
      Contact
      <Icon class='fill-clr-n3 w-5'
        name='pencilPaper' />
    </a>

</nav>